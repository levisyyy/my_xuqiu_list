{
  "name": "prd-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "产品需求列表管理 — 单文件周×天进度网格（交付物为 index.html，本 package.json 仅供测试用）",
  "scripts": {
    "test": "node test/syntax.check.js && node test/smoke.test.js"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
